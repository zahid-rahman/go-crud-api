{
  "name": "golang-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "go": "nodemon --exec go run main.go --signal SIGTERM"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
